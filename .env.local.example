# RAWGLE Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# ============================================
# Google Places API (REQUIRED)
# ============================================
# Get your keys from: https://console.cloud.google.com/apis/credentials
# These variable names have been renamed for security after a key leak
GOOGLE_PLACES_API_KEY_TENSHIMATT=your_primary_google_places_api_key_here
GOOGLE_PLACES_API_KEY_SAMCO=your_backup_google_places_api_key_here
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your_primary_google_places_api_key_here

# ============================================
# OpenAI API (REQUIRED for AI features)
# ============================================
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# NextAuth (REQUIRED for authentication)
# ============================================
# For development
NEXTAUTH_URL=http://localhost:3005
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=your_random_secret_here_generate_with_openssl

# For production (add to Vercel)
# NEXTAUTH_URL=https://your-domain.vercel.app

# ============================================
# Stripe (REQUIRED for payments)
# ============================================
# Get your keys from: https://dashboard.stripe.com/apikeys
# Publishable key (safe to expose on client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
# Secret key (server-side only, NEVER expose to client)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
# Webhook secret from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Base URL for redirects
NEXT_PUBLIC_BASE_URL=http://localhost:3005

# ============================================
# Supabase (REQUIRED for database)
# ============================================
# Get from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================
# Optional: Database (if not using Supabase)
# ============================================
# DATABASE_URL=postgresql://user:password@host:port/database

# ============================================
# Optional: Analytics
# ============================================
# NEXT_PUBLIC_GA_MEASUREMENT_ID=your_google_analytics_id
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=your-domain.com

# ============================================
# Gelato Print-on-Demand (REQUIRED for POD features)
# ============================================
# Get your API keys from: https://dashboard.gelato.com/
# Environment: 'sandbox' for testing, 'production' for live orders
GELATO_ENVIRONMENT=sandbox
# Sandbox API key (for testing)
GELATO_SANDBOX_API_KEY=your_gelato_sandbox_api_key_here
# Production API key (ONLY use in production!)
GELATO_API_KEY=your_gelato_production_api_key_here

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. NEVER commit this file to git (.env.local is in .gitignore)
# 2. Store a backup copy in a password manager (1Password, LastPass, etc.)
# 3. Add all production keys to Vercel dashboard (different values!)
# 4. Rotate keys regularly (every 90 days recommended)
# 5. Use different keys for development and production
# 6. Add API restrictions in Google Cloud Console:
#    - Restrict to specific APIs (Places API, Geocoding API)
#    - Add HTTP referrer restrictions for production
# ============================================
